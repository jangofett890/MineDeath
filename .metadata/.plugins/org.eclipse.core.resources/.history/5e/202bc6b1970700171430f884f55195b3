package commands;

import org.bukkit.entity.Player;

import gamesarena.Arena;
import gamesarena.ArenaManager;
import lang.Lang;

public class Join extends SubCommand{
	ArenaManager arenaManager = ArenaManager.getInstance();
	public void onCommand(Player player, String[] args){
		if (arenaManager.getArena(player) != null){
			sendMessage(player, "You are already in a game", Lang.Status.INFO);
			return;
		}
		int id = 0;
		try{ id = Integer.parseInt(args[0]); }
		catch (Exception e){sendMessage(player, args[0] + " is not a valid number", Lang.Status.ERROR); return;}
		Arena a = ArenaManager.getInstance().getArena(id);
		if (a == null){
			sendMessage(player, "There is no arena with the Id: "+ id, Lang.Status.ERROR);
		}
	}
	public String name(){
		return "join";
	}
	public String info(){
		return "Join a game";
	}
	public String[] aliases(){
		return new String[]{"J"};
	}
}
